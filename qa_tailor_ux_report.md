# تقرير اختبار تجربة المستخدم (UX/UI QA Report) - مسار الخياط (المعلم)

بناءً على الفحص المعمق لأكواد واجهات المستخدم (React Components) الخاصة بمسار الخياط (`OrdersBoard.jsx`, `Alterations.jsx`, `TailorPos.jsx`, `MeasurementCapture.jsx`)، تم استخراج نقاط الاحتكاك (Friction Points) التالية التي تواجه "المعلم" أو الخياط أثناء الاستخدام اليومي:

## 1. شاشة المعمل / لوحة الإنتاج (`OrdersBoard.jsx`)
* **الاحتكاك في نقل الحالات (Stage Transitions):**
  * **المشكلة:** يتم نقل الطلب من "قص" إلى "خياطة" عبر النقر على أزرار صغيرة (تقديم ← / رجوع). الخياط (المعلم) غالباً ما تكون يداه مشغولة بالقماش أو المتر، والنقر على أزرار صغيرة على شاشة اللمس (التابلت) يعتبر مزعجاً وبطيئاً.
  * **التوصية:** دعم خاصية السحب والإفلات (Drag & Drop) للبطاقات، أو إضافة إيماءات السحب (Swipe Left/Right) على البطاقة لنقلها للمرحلة التالية.
* **غياب التحديث الجزئي للقطع (Partial Garment Advance):**
  * **المشكلة:** إذا كان الطلب يحتوي على 3 أثواب، وضغط الخياط على "تقديم"، يتم نقل **جميع الأثواب** الموجودة في تلك المرحلة إلى المرحلة التالية دفعة واحدة عبر حلقة تكرارية (`forEach(g => handleStageChange)`). لا توجد واجهة لنقل ثوب واحد فقط في حال تأخر باقي الأثواب.
* **الاستجابة للشاشات (Responsiveness):**
  * **المشكلة:** الأعمدة ثابتة العرض (`minWidth: 290px`) وتعتمد على التمرير الأفقي (`overflowX: auto`). على شاشات الهواتف أو التابلت في الوضع العمودي (Portrait)، سيكون التمرير الأفقي المستمر مرهقاً.

## 2. إدارة التعديلات (`Alterations.jsx`)
* **غياب التراجع في حالات التذكرة (One-way State Mutation):**
  * **المشكلة:** دورة حياة تذكرة التعديل تم برمجتها في اتجاه واحد فقط: (`pending` ← `in_progress` ← `ready` ← `delivered`). لا يوجد زر "تراجع". إذا ضغط الخياط بالخطأ على "تعليم كجاهز ✔"، لا يمكنه إعادتها إلى "جاري العمل".
  * **التوصية:** إضافة زر تراجع أو قائمة منسدلة (Dropdown) لتغيير الحالة بحرية في حال الأخطاء البشرية.
* **تصميم نموذج التعديلات المتجاوب (Modal Responsiveness):**
  * **المشكلة:** عند إضافة قطع متعددة في شاشة إضافة طلب تعديل جديد، يتم عرض (نوع القطعة، التعليمات، السعر) في سطر واحد باستخدام `display: flex`. على الشاشات الصغيرة، ستتداخل الحقول، وزر الحذف `X` يمتلك `marginTop: 24px` ثابت مما سيؤدي إلى تشوه المظهر إذا التفت الحقول (Wrapped).
* **إيصال التعديل (Print Ticket):**
  * **المشكلة:** الفاتورة الحرارية الخاصة بالتعديلات تعتمد على HTML بسيط جداً ولا تحتوي على **باركود (Barcode)** لتذكرة التعديل. عند عودة العميل لاستلام ثوبه، سيضطر الموظف للبحث برقم الجوال أو الاسم بدلاً من مسح الباركود السريع.

## 3. شاشة أخذ المقاسات ونقاط البيع (`TailorPos.jsx` & `MeasurementCapture.jsx`)
* **عدم اتساق وحدات القياس (Unit Inconsistency):**
  * **المشكلة:** في صفحة `MeasurementCapture.jsx` (الملف الشخصي للمقاسات)، يوجد زر رائع للتبديل بين (إنش / سم) ويقوم بتحويل القيم برمجياً. **لكن**، في شاشة `TailorPos.jsx` (وهي الشاشة التي يستخدمها الموظف لإنشاء الطلب السريع)، حقول المقاسات مثبتة حرفياً على "إنش" (Hardcoded "إنش" placeholder). إذا كان المعلم يستخدم شريط قياس بالسنتيمتر، سيضطر للتحويل ذهنياً.
* **الاحتكاك في اختيار الخياط:**
  * **المشكلة:** الحقل المخصص لاختيار المعلم `<select id="tailorSelect">` يطلب من المستخدم اختيار الخياط يدوياً في كل طلب جديد.
  * **التوصية:** يجب أن يتم التحديد التلقائي (Auto-select) للخياط إذا كان هو نفسه من قام بتسجيل الدخول للنظام (بناءً على الـ `staff_id`).
* **ازدحام الشاشة (UI Clutter):**
  * **المشكلة:** الشاشة مقسمة لـ 3 أعمدة. العمود الثالث "معاينة مباشرة لشكل ورقة الطباعة" يأخذ مساحة كبيرة من الشاشة طوال الوقت (`order: 3`). على أجهزة التابلت، هذا يضغط عمود "المقاسات" وعمود "بيانات العميل" ويجعل الأزرار أصغر.
  * **التوصية:** إخفاء عمود "المعاينة" خلف زر (Modal) أو تبويب (Tab)، لإعطاء مساحة أوسع لإدخال المقاسات المريحة للعين.
* **اختيار تاريخ التسليم:**
  * الأزرار السريعة ("3 أيام"، "7 أيام") ممتازة جداً وتقلل الاحتكاك، لكن حقل `<input type="date">` الأساسي يكون استخدامه سيئاً على أجهزة الآيباد والتابلت، يفضل استخدام مكتبة تقويم مخصصة (Custom Datepicker) مستقبلاً.
